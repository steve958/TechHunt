import React from "react";

export const ctx = React.createContext(null)
export const Provider = ctx.Provider

export const ctxBlue = React.createContext(null)
export const blueProvider = ctx.Provider